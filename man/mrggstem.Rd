% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mrggstem.R
\name{mrggstem}
\alias{mrggstem}
\alias{ggplot_add.mrggstem}
\title{Attach an output file stem to a plot}
\usage{
mrggstem(stem)

\method{ggplot_add}{mrggstem}(object, plot, ...)
}
\arguments{
\item{stem}{a single string to form the name of the output file.}

\item{object}{a `mrggstem` object.}

\item{plot}{the plot the stem is being added to.}

\item{...}{not used.}
}
\description{
Use this function in a \code{ggplot2} pipeline (with \code{+}) to store the stem
for the output file name in the plot object itself.
}
\details{
The stem is saved in the plot object as \code{mrggsave.stem} and can be
retrieved with \code{p$mrggsave.stem}. Under \code{ggplot2} 4.0.0 and later, this
is stored in the \code{meta} slot of the \code{gg} object.
}
\examples{
library(ggplot2)

p <- ggplot(mtcars, aes(wt, mpg)) + geom_point() + mrggstem("wt_vs_mpg")

p$mrggsave.stem

}
\seealso{
\code{\link[=mrggsave]{mrggsave()}}
}
